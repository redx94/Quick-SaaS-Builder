version: '3.7'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
      target: backend
    container_name: quick-saas-backend
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://user:password@db:5432/mydb
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    ports:
      - "5000:5000"
    volumes:
      - ./src:/app/src  # Sync local changes to the container for development

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      target: frontend
    container_name: quick-saas-frontend
    environment:
      - NODE_ENV=production
    ports:
      - "3000:80"
    volumes:
      - ./frontend:/frontend  # Sync local changes to the container for development

  nginx:
    image: nginx:alpine
    container_name: quick-saas-nginx
    ports:
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf  # Use custom Nginx configuration
      - ./path/to/ssl/cert.pem:/etc/ssl/certs/cert.pem
      - ./path/to/ssl/key.pem:/etc/ssl/private/key.pem
      - /usr/share/nginx/html  # Frontend build files from frontend service
    depends_on:
      - frontend

  db:
    image: postgres:13
    container_name: quick-saas-db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: mydb
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data  # Persist data in a named volume

volumes:
  postgres_data:
